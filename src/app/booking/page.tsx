/**
 * Booking Page — Management Hub (placeholder).
 * Will contain active reservations, QR receipt, and booking history.
 */

export default function BookingPage() {
  return (
    <section>
      <h1 className="text-lg font-bold text-neutral-100">Booking Management</h1>
      <p className="mt-2 text-sm text-neutral-400">
        Reservasi aktif, timer, dan riwayat booking akan ditampilkan di sini.
      </p>
    </section>
  );
}
