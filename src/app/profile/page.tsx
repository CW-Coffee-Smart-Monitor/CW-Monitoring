/**
 * Profile Page — Identity & Insights (placeholder).
 * Will contain RFID sync, productivity analytics, and settings.
 */

export default function ProfilePage() {
  return (
    <section>
      <h1 className="text-lg font-bold text-neutral-100">Profile & Settings</h1>
      <p className="mt-2 text-sm text-neutral-400">
        RFID sync, analytics produktivitas, dan pengaturan akan ditampilkan di sini.
      </p>
    </section>
  );
}
