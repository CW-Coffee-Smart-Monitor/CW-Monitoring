/**
 * Map Page — Interactive Floor Plan (placeholder).
 * Will contain SVG-based layout with clickable tables.
 */

export default function MapPage() {
  return (
    <section>
      <h1 className="text-lg font-bold text-neutral-100">Interactive Floor Plan</h1>
      <p className="mt-2 text-sm text-neutral-400">
        Denah interaktif CW Coffee akan ditampilkan di sini.
      </p>
    </section>
  );
}
